const config = {
    db: {
      /* don't expose password or any sensitive info, done only for demo */
      host: "localhost",
      user: "MainAdmin",
      password: "user",
      database: "eshop",
    },
  };
  module.exports = config;